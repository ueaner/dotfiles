# vim: set ft=cfg:

# ansible-inventory --list
# ansible svr -m ping

# The `desktop` variable for the `local` group is predefined in ~/.ansible/group_vars/local.
[local]
localhost

[local:vars]
ansible_connection=local

[lan]
cb13     desktop=Sway
mac14    desktop=Sway
mac15    desktop=Aqua
mac14hot desktop=GNOME

[termux]
mi11
mi11hot

[ssserver]
ss

[spserver]
sp
