# Read `man 5 sway` for a complete reference.

### Variables

# Mod4 for Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

set $term foot
set $term-app-id foot
set $term-float $term --app-id='term-float'
# set $term alacritty --option font.size=10.00
# set $term-app-id Alacritty
# set $term-float $term --class 'term-float'
set $lock swaylock -f

# Note: pass the final command to swaymsg so that the resulting window can be opened
# on the original workspace that the command was run on.
set $rofi rofi \
    -run-command 'swaymsg exec -- {cmd}' \
    -run-shell-command 'swaymsg exec -- {terminal} -e {cmd}' \
    -terminal '$term'

### Include configs

include '$(/usr/libexec/sway/layered-include "/usr/share/sway/config.d/*.conf" "/etc/sway/config.d/*.conf" "${XDG_CONFIG_HOME:-$HOME/.config}/sway/config.d/*.conf")'
# Include host-specific configs
include "${XDG_CONFIG_HOME:-$HOME/.config}/sway/config.d/host-specific/$(hostnamectl hostname).conf"
