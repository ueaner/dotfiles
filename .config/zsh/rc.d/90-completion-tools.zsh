# python3 -m site
# pip3 config list
# source <(python3 -m pip completion --zsh)
#
# FPATH 中自定义了补全路径，直接输出到文件
# 使用的时候再去查找，而不是直接加载到 zsh 进程内存
# python3 -m pip completion --zsh > ~/.local/share/zsh/site-functions/_pip


